import React from 'react';
import Todo from './TodoForm.js';

const TodoForm = () => {

    const handleChange = () => {
        prevent.default();
        onSubmit = (e) =>{
        }
    }

    const handleSubmit = (e) => {
        
        [...newTodo, ]


    }

    return(

        <div>
            <form onSubmit={handleSubmit}>



            </form>
        </div>


    )
}
